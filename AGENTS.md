# 发布

发布必须执行 `bash release.sh`；如果当前终端是 PowerShell，则执行 `& 'C:\Program Files\Git\bin\bash.exe' .\release.sh`。不要直接运行 `git add`、`git commit` 或 `git push`。

# 内容规划与去重

每次推荐“下一步内容”前，必须先检索中英文现有页面、已存在的预览文件和当前计划文件；标题相近不够，必须核对用户场景、操作步骤和结论是否已经覆盖。

每个推荐必须同时写清：

1. 已有覆盖：文件路径和具体区块；
2. 真实缺口：现有内容为什么仍不能解决该用户问题；
3. 改动位置：将修改哪个页面、哪个区块；
4. 可见结果：用户打开页面后能一眼看到的变化。

同一用户问题已有充分覆盖时，禁止把“补内容”列为下一步。若只是把已有内容挪到首页、调整入口或重排版，必须明确标为导航/排版改造，不得说成新增内容。

`task_plan.md`、`progress.md` 和预览文件只用于记录，不是页面现状的证据；每次给建议前都必须重新核验实际页面。发现推荐与页面事实冲突时，当轮先撤销或更正错误计划项，再提出新的候选。没有明确可见结果的工作不得排在下一步第一优先级。
